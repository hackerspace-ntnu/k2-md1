add_subdirectory ( cl )
#add_subdirectory ( fusion )
#add_subdirectory ( newclstuff )
#add_subdirectory ( reconstruct )
#add_subdirectory ( tracker )

add_executable ( FirstKinect firstkinect.cpp )
add_executable ( PointCloud pointcloud.cpp )
add_executable ( RenderSync rendersync.cpp )
add_executable ( SyncDepth syncdepth.cpp )
add_executable ( WrapperTest wrappertest.cpp )

target_link_libraries ( FirstKinect 
	${DEFAULT_LIBS} )
target_link_libraries ( PointCloud
	${DEFAULT_LIBS} )
target_link_libraries ( RenderSync
	${DEFAULT_LIBS} )
target_link_libraries ( SyncDepth
	${DEFAULT_LIBS} )
target_link_libraries ( WrapperTest
	${DEFAULT_LIBS} )
